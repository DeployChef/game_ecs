namespace Game.Domain.ECS;

/// <summary>
/// Маркерный интерфейс для всех компонентов в ECS.
/// 
/// Почему интерфейс, а не абстрактный класс?
/// - Компоненты могут быть struct (value types) - быстрее, меньше аллокаций
/// - Интерфейс позволяет использовать и class, и struct
/// 
/// Почему пустой интерфейс?
/// - Это маркер для типобезопасности
/// - World будет проверять: "это компонент?" через "where T : IComponent"
/// - Не навязывает никакого поведения компонентам
/// </summary>
public interface IComponent
{
    // Пустой интерфейс - только маркер
    // Все данные компонента - это его поля/свойства
}

